# Environment Variables Template
# Copy this file to .env.local and fill in your values
# Never commit .env.local to version control!

# Application Configuration (Safe - these can be public)
VITE_APP_NAME="COVID-19 Dashboard"
VITE_APP_VERSION="1.0.0"

# API Configuration (Safe - public APIs)
VITE_API_BASE_URL="https://disease.sh/v3/covid-19"
VITE_API_TIMEOUT="5000"

# Feature Flags (Safe - configuration only)
VITE_ENABLE_MOCK_DATA="true"
VITE_ENABLE_ANALYTICS="false"
VITE_ENABLE_DEBUG_MODE="false"

# Build Configuration (Safe)
VITE_BUILD_TARGET="es2015"

# SECURITY NOTES:
# - Only variables prefixed with VITE_ are exposed to the client
# - Never put sensitive data like API keys, secrets, or passwords here
# - This COVID-19 API is public and doesn't require authentication
# - All VITE_ variables will be visible in the compiled JavaScript
# - Use server-side environment variables for sensitive data